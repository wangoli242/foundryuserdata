export const log = (message) => console.log(`Foundry VTT | Lancer Clocks | ${message}`);
export const warn = (message) => console.warn(`Foundry VTT | Lancer Clocks | ${message}`);
export const error = (message) => console.error(`Foundry VTT | Lancer Clocks | ${message}`);
